{
  "name": "victim-func",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start"
  },
  "dependencies": {
    "mongodb": "^5.7.0",
    "@azure/functions": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "typescript": "^4.0.0",
    "azure-functions-core-tools": "^4.x"
  }
}